# 易經老師本機運算

2026-09-24：依使用者要求，Google 生成暫停；老師對外名稱為「易經老師」。

- `lib/teacher-provider.ts` 統一停止取得 Google 生成金鑰，既有密鑰不刪除、不外洩。
- 八字兩個既有解讀路由直接使用易經本機規則，回傳 `source: local-iching`、`provider: 易經老師`。
- 紫微三種老師直接使用既有命盤與易經規則；娛樂解讀沿用本機象徵敘事，保留虛構聲明。
- 這是規則式解讀，不是部署了本機大型語言模型。前端負責呈現後端結果。
- 有本機替代的其他功能沿用本機結果；只有外部生成的歌曲音訊、故事等功能暫停，不能宣稱全部生成服務均可用。
- 可用性測試實際執行後端函式，阻擋 Google SDK 與 fetch 外送，檢查輸出、資料不足與一致性。它不代表 Google 連線可用，也不替代正式站驗證。

驗證指令：`node tests/ai-teacher-availability.mjs`、`npx tsc --noEmit`、`npm run health:check`。

恢復 Google 前須由使用者決定，檢查供應商額度與相關行為測試；不能只改顯示名稱就當作恢復完成。
