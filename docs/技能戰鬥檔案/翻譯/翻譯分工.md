# 翻譯分工（韓文一條龍 × 介面多語）

更新：2026-09-25 07:08（UTC+8），由「命理」助手依使用者最新指示改寫，用來和另一位工程師對齊，避免互相覆蓋。
**請另一位工程師看過後在「確認」欄簽註；未確認前，共用檔案我不動。**

## 範圍調整（使用者指示）
- 命盤解說「英文」改由你（介面多語工程師）負責，我不做英文。
- 我只負責「韓文（ko）」，而且是全站一條龍：首頁、配對表單、確認頁、命盤解說結果頁與其他前台可見文字。

## 你負責，我不碰
- `components/InterfaceLanguage.tsx`、`lib/interface-languages*`（語言狀態、選單）
- 英文、日文、簡體字典：`lib/home-english-copy.ts`、`lib/form-english-copy.ts`、`lib/birthday-english-copy.ts`、`lib/home-japanese-copy.ts`，以及命盤解說英文
- 配對表單、驗證訊息、兩人資料確認頁的程式（英文已完成並通過測試，不重做）
- `lib/translation-service.ts`、動態後端翻譯服務、`scripts/audit-interface-copy.cjs`、`tests/translation-service.test.cjs`
- 本資料夾其他文件（`整頁翻譯驗收.md` 等）

## 我（命理助手）負責
- 新資料夾 `lib/korean-copy/`（home、form、birthday、result、pages、index），以中文原句為鍵、韓文為值，格式和你的字典一致
- 只翻可讀文字；姓名、出生資料、四柱原字、數值與所有計算邏輯一律不動

## 共用、需先對齊才改
1. `components/HomeTranslatedText.tsx`：語言為 ko 時查 `koreanCopy`，查不到就顯示原文（一行分支，不影響 en／ja）
2. 尚未包進 `HomeTranslatedText`／`useDisplayText` 的畫面字串（例如配對結果區塊、其他頁）：需要包起來才能顯示任何外語，這也會讓你的英文受益
以上我只準備修改稿（放在 `docs/技能戰鬥檔案/翻譯/proposals/`），等你確認後才套用；你要自己接也可以。

## 用詞對齊
韓文依你的英文語意翻，專有詞保留原字：팔자(八字)=Ba Zi、자미두수(紫微斗數)=Zi Wei、사주=four pillars、오행=five elements、1번 사람／2번 사람=Person 1／Person 2、자(子)시=Zi (子)、A형=Type A。

## 確認
- 介面多語工程師：＿＿＿＿（日期：＿＿）

## 追加（2026-09-25 07:10 UTC+8，使用者指示）：日文接在韓文之後
- 韓文完成後，我接著做全站日文。
- `lib/home-japanese-copy.ts` 仍是你的檔案，我不改。我新開 `lib/japanese-copy/`，只放你的日文字典還沒有的字串；同一個中文鍵以你的譯文為準。
- 日文接上畫面同樣要在 `HomeTranslatedText.tsx` 的 ja 分支多查一層（先查你的，再查我的），修改稿一樣先給你確認。

## 追加（2026-09-25 07:10 UTC+8，使用者直接指示）：開始實際接線
- 使用者要我現在就把首頁 `app/page.tsx`、之後配對頁 `app/match/page.tsx` 的寫死中文包進 `HomeTranslatedText`／`useDisplayText`，並擴充 `HomeTranslatedText.tsx` 的查找鏈（en：form→home→site；ja：home→site；ko：site；zh-Hans：site）。
- 新字串只放在新檔 `lib/site-copy/en.ts`、`ja.ts`、`ko.ts`、`zh-Hans.ts`；你的字典檔不改，同一句以你的譯文為準。
- 每次改檔前後都比對檔案修改時間，如果你剛存過，我會在你的版本上重套，不會覆蓋你的修改。計算邏輯不動、不提交。

## 介面多語端已確認：共用檔案單一寫入者（2026-09-25）

已讀取上述最新分工與「開始實際接線」追加項。介面多語端同意以下協作界線：

- 韓文／日文接線端負責 `app/page.tsx`、`app/match/page.tsx`、`components/HomeTranslatedText.tsx` 的後續文字包裝與字典查找鏈，以及 `lib/korean-copy/`、`lib/japanese-copy/`、`lib/site-copy/`。介面多語端自本確認起不再同時編輯這些共用檔案。
- 介面多語端負責原有英文專用字典、生日英文顯示、後端翻譯服務與服務測試；新增英文文案若屬 site-copy，先以提案交由接線端整合，不同時改同一檔。
- 配對表單最新版本已有英文欄位、展開選項、錯誤提示、下一步與兩人確認頁；本機瀏覽器檢查通過，姓名與生日值保留。請以目前磁碟內容為基礎接線，不還原先前版本。
- 修改時間檢查只能發現風險，不能取代分工。需要跨入對方負責檔案時，先在本文件記錄移交並取得確認；不要整檔覆寫或重跑舊版批次腳本。
- 統一由介面多語端在所有寫入停止後檢視差異、完成跨頁語言與健康檢查、再提交推送；接線端不自行提交或推送。
- 共同验收：選一次語言，首頁、填表、錯誤、確認、展開內容、命盤標籤、解說與朗讀均跟隨。原文回退代表缺漏，不能算已翻完；計算骨架、原始資料和數值不變。

確認：介面多語端已確認上述分工。韓文／日文接線端尚待讀取本追加並確認；在確認前不宣稱雙方已完成對焦。

## 生日元件移交確認（2026-09-25）
- 依本次直接對話，生日元件 `components/LunarBirthdayInput.tsx` 的韓文接線交由韓文端修改；介面多語端暫停編輯該元件，保留既有英文與日期運算。
- 韓文端一併處理生日動態成功／錯誤提示，不能只接 23 筆固定文案便宣稱完成。命盤結果與其他頁韓文字典繼續由韓文端填寫。
- 英文血型說明由介面多語端處理：已補四筆含「民間說法：」的完整原文鍵，英文保留 Folk belief 限定，不改原文常數或血型運算。韓文端不再重複修改英文字典。

## 追加（2026-09-25 07:15 UTC+8，使用者指示）：韓文第二輪分工
### 命理負責
1. `components/LunarBirthdayInput.tsx`：只加韓文分支，和英文同一層，涵蓋日期確認和錯誤提示。最小修改，不重構。
2. 韓文字典：
   - 命盤結果：`lib/korean-copy/result.ts`（配對結果區、結果元件、雙命盤結果）
   - 其他頁面：`lib/korean-copy/pages.ts` 加上 `lib/korean-copy/pages/`，每頁一個檔。順序是八字、姓名學、數字、星座、紅鸞、塔羅、紫微、音樂、星宿神獸、成長中心，接著共用元件，最後是神獸戰鬥。後台和測試頁不翻。
3. 已審過的 205 句韓文（home、form、birthday）不修改。

### 你負責，命理不碰
- `lib/form-english-copy.ts` 的四句英文血型說明，含「民間說法：」開頭，已由你修正並驗收通過。命理不重做，也不重疊。
- 首頁、配對頁及其他頁面程式檔，仍屬你的檔案。需要包進翻譯元件的行號清單，命理會另外列給你。

### 共同規則
計算邏輯不動，不提交、不推送。每次存檔前先檢查對方檔案的最新修改時間，如有更新就在最新版本上重新套用，不覆蓋。

## 英文現況（2026-09-25 07:19 UTC+8，使用者口述記錄；英文由你負責，命理不動）
- 英文結果字典 `result-english-copy.ts` 目前還是空的，全套驗收也還沒跑。
- 翻譯服務還沒部署。
- 另外英文這邊，配對表單和確認流程也還有缺漏。
- 其他頁面的結果顯示和完整英文也還沒補上。
- 以上都屬於英文工程師的工作，命理只負責韓文（result.ts、pages/、生日元件的韓文分支）。

## 英文互補加速（依最新直接對話）
- 另一端新增 `lib/english-copy/` 全站英文補充字典；既有 home/form/birthday/result 英文字典由介面多語端保留，不重寫，四句血型與五元素 25 段英文原樣保留。
- 介面多語端只接目前已存在的補充字典，負責頁面顯示接線、後端服務與整合驗證；不編輯 `lib/english-copy/pages/`，避免與補翻同步寫入。
- 每批補翻可先交付獨立頁字典，不必等全站完成才接畫面；型別通過不等於全頁翻譯完成，保留漏翻清單。
- 更正舊狀態：`lib/result-english-copy.ts` 現已建立，50 處固定文字與 16 個結果顯示欄位已接線，模擬配對結果測試通過；非全站完成。

## 修訂分工（2026-09-25 07:29 UTC+8，使用者指示，以此為準）
2026-09-25 07:29 修訂分工：全站英文字典（所有頁面、命盤結果、配對表單、確認流程、共用元件、神獸戰鬥）由對方負責，命理不寫任何英文字典檔。命理負責：(1) 畫面接線——把英文字典接到畫面；(2) 驗證——型別檢查、結果頁測試、手機 390 截圖。韓文（lib/korean-copy/**、result.ts、pages/、生日元件）照常並行。不動計算、不 commit/push、每次寫入前重新確認對方存檔。

- 清理紀錄（07:31）：命理端先前產生的 `lib/english-copy/pages/bazi.ts`（07:28:39 建立）已移除，`lib/english-copy/` 資料夾已刪；`components/HomeTranslatedText.tsx` 中對應的 `enBaziCopy` 匯入與兩處查找已拿掉，其餘（含 ko 分支）不變。上方「英文互補加速」一節提到的 `lib/english-copy/` 補充字典因此不再存在；英文補充內容請直接放在你的英文字典檔。
- `lib/result-english-copy.ts`、`lib/form-english-copy.ts`（含四句「民間說法：」血型說明）、`lib/home-english-copy.ts`、`lib/birthday-english-copy.ts` 未更動。

## 最新三段分工與配對頁移交（依目前直接對話）
- 三段驗收：一、首頁入口；二、填寫資料與確認流程；三、後端運算後顯示的命盤、解說與結果。不能只以前兩段驗收代替第三段。
- 配對頁 `app/match/page.tsx` 自本追加起交由英文結果接線端單獨寫入；可以接英文結果顯示及剩餘「返回首頁」文字，不改表單與確認流程邏輯。根任務停止寫入此檔。
- 根任務目前負責韓文結果唯讀核對與缺漏回報；未另行移交前不修改上述共用頁面、不修改對方正在填寫的字典。
- 既有英文結果接線已保存磁碟，請以目前內容做局部補丁，不覆寫整檔。計算引擎、資料結構、姓名、日期與數值原樣保留。
- 「計算規則和數值」屬於保護與比對範圍，不是改動範圍。

## 使用者文案修正：二十八是分類數（2026-09-25）
- 使用者明確指定：「二十八」是分類數，不是張數。後續首頁與各語翻譯不得把此處寫成二十八張卡。
- 待整合核對首頁「28 張本體、28 張幼子、4 張四象，共 60 種神獸」整句，以分類／種類為單位，與實際本體、幼子及四象清單逐一對照。
- 本次只記錄顯示文案語義，不改收藏數量、發卡獎勵、角色資料或遊戲規則；例如首戰領取卡片的張數需另依實際功能核對，不能全部取代。
