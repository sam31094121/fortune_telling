# 專案 Git 工作規則

此專案採單一主線工作方式：只使用 `main`。

- 所有日常修改都直接在 `main` 完成。
- 完成一批修改後，先確認內容，再提交並推送到 `origin/main`。
- 不建立或保留功能分支、整合分支、臨時分支或驗證用 worktree，除非使用者明確要求。
- 工作完成時必須執行 `git status --short --branch`；結果應只顯示 `main` 已與 `origin/main` 同步，且沒有未提交檔案。
- 若因必要作業暫時建立分支或 worktree，完成後必須先整合、推送，再移除它們，回復為只有 `main` 的乾淨狀態。

