# 專案 Git 工作規則

此專案採單一主線工作方式：只使用 `main`。

- 所有日常修改都直接在 `main` 完成。
- 永久採用「完成即自動更新」：每批修改完成、內容確認且相關測試與健康檢查通過後，立即提交並推送到 `origin/main`，不再等待使用者重複確認。
- 若測試失敗、存在未解衝突、修改範圍或風險尚未釐清，禁止自動更新；先停止並向使用者回報。
- 不建立或保留功能分支、整合分支、臨時分支或驗證用 worktree，除非使用者明確要求。
- 工作完成時必須執行 `git status --short --branch`；成功自動更新後，結果應只顯示 `main` 已與 `origin/main` 同步，且沒有未提交檔案。若因安全門檻未更新，須如實回報未提交變更。
- 若因必要作業暫時建立分支或 worktree，完成且驗證通過後須整合、推送並移除它們，回復為只有 `main` 的乾淨狀態。

