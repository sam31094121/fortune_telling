# 專案 Git 工作規則

此專案採單一主線工作方式：只使用 `main`。

- 所有日常修改都直接在 `main` 完成。
- 完成一批修改後，先確認內容；只有使用者明確要求「保存修改」或提交／推送時，才提交並推送到 `origin/main`。
- 不建立或保留功能分支、整合分支、臨時分支或驗證用 worktree，除非使用者明確要求。
- 工作完成時必須執行 `git status --short --branch`；若使用者要求保存修改，結果應只顯示 `main` 已與 `origin/main` 同步，且沒有未提交檔案。未要求保存時，須如實回報未提交變更。
- 若因必要作業暫時建立分支或 worktree，完成後須在使用者要求保存修改時才整合、推送並移除它們，回復為只有 `main` 的乾淨狀態。

